# polish-wire
spec: Final integration seams per DESIGN_SPEC: (1) deck drill-through — run-focus view and ⌘K palette get "open deck for this run" entries linking /deck?run=<id>; gate-card evidence labels become live links where targets exist. (2) Desk chime — audible tone from the open tab on gate-raise/fail/complete (Web Audio, no assets), with a visible mute toggle, defaults respecting prefers-reduced-motion. (3) Phone restack audit — verify/fix single-column <1024px stacking + touch affordances on /deck and /graph (run-focus already done). (4) /brand route — 3 product-name proposals rendered as Oxanium wordmarks on CRT tokens, one-line rationale each; operator picks later, no renaming now. (5) Make Project.id opaque (stable slug/hash) in lib/server/discovery.ts + all dependents (routes, graph identity matching, deck diff resolution) — id must no longer be a filesystem path in client payloads. (6) NTFY_DEEPLINK_BASE (or equivalent) env for notifier deep links instead of hardcoded host assumptions.
owns: console/** (single cross-cutting lane)
acceptance: cd console && npx vitest run && npm run build && npm run lint all pass; SSR smoke /, /run/<id>, /deck, /graph/<p>, /brand all 200; fixture SSE unchanged.
read: DESIGN_SPEC.md §3 (name proposals), §4 (nav/palette), §5 (chime channel), §6; NOTES.md Batch B follow-ups.
