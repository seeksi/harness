// web/app/page.tsx — (C) placeholder. Lane C owns this: the idle submit surface ⇄
// running pipeline (two states, one screen). Lane 0 ships the idle render only.
export default function Page() {
  return (
    <main>
      <h1>Umbrella — idle</h1>
    </main>
  );
}
