# foundation
spec: Build the complete foundation of the DESIGN_SPEC.md dashboard as a new Next.js app in console/: app skeleton (package.json, configs), industrial-CRT design tokens (graphite base, amber phosphor interface voice, green=live/healthy only, dark-only, WCAG 2.2 AA) + Geist/Geist Mono + one display face; provider-agnostic multi-run event contract (runId/projectId/agentId envelope) + reducer + store; project auto-discovery over a named-roots list; SQLite persistence with 20-runs-per-project retention; SSE stream endpoints with reconnect/replay; ntfy notifier module (gate raised / run failed-stuck / run completed); fleet home view (2-3 run lanes, ops board when idle), phase-rail component, launch console (project+brief required, model-routing optional), command palette. Fixture producer drives the UI; live bridge is Batch B+.
owns: console/** (entire directory; do not touch web/)
acceptance: cd console && npm run build passes AND npx vitest run passes (reducer, discovery, retention, notifier unit tests) AND fleet home renders fixture lanes at localhost.
read: /home/alter/HARNESS/DESIGN_SPEC.md (the full spec — §3 visual system, §5 components, §6 data/metrics are binding). Reference-only: web/ (old Umbrella; reuse ideas, not files).
