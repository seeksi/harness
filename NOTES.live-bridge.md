# live-bridge
spec: Real event producer replacing fixture-only runtime: POST /api/runs {projectId, brief, routing?} spawns harness.sh phases as a single-slot-per-lane orchestrator — typed argv from server-minted enums ONLY (port the security pattern of web/lib/daemon/harness-bridge.ts: spawn shell:false, per-event-type schema whitelist on stdout parse, SQLite audit per spawn; read its threat model docs/security/threat-model-harness-bridge.md). Wire events -> persist.appendEvent + SSE broadcast + notifier (gate-raised/failed-stuck/completed w/ deep link). Fix follow-ups: wall-clock nowSec into deriveHealth (RunLane.tsx); gate-id-aware onApprove (FleetHome.tsx); per-run events cap + LIMIT/pagination on eventsSince (persist.ts); batch the /api/projects N+1. Live mode behind HARNESS_LIVE=1, fixture remains default when unset.
owns: console/lib/server/**, console/lib/bridge/**, console/app/api/**, console/components/RunLane.tsx, console/components/FleetHome.tsx
acceptance: cd console && npx vitest run passes (new: argv typing/injection rejection, stdout schema whitelist, notifier firing on transitions, retention/LIMIT) && npm run build passes && with HARNESS_LIVE unset the app still runs fixture exactly as before.
read: DESIGN_SPEC.md §6 (event-source scope, degraded), web/lib/daemon/harness-bridge.ts + registry.ts (pattern to port, do NOT import from web/), docs/security/threat-model-harness-bridge.md.
